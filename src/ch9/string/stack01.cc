#include <deque>
#include <string>
#include <stack>
#include <iostream>
using namespace std;

int main()
{
  deque<int> num{1,2,3,4};
  stack<int> s(num);
  
  stack<string, vector<string>> str_stk;
  stack<string, vector<string>> str_stk2(svec);


  return 0;
}
