#include <iostream>
#include <string>

using namespace std;

int main()
{
  
  string s = "word";
  int a = 88;
  cout << s + a << endl;
  //  string pl = s + s[s.size() - 1] == 's' ? "" : "s" ;
  
  return 0;
} 
