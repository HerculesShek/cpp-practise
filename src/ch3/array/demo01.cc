#include <iostream>
#include <cstddef>
using namespace std;

unsigned scores[11] = {};
unsigned as[11];
int main()
{
  for(auto a : scores)
	cout << a << endl;
  
  for(auto a : as)
	cout << a << endl;
  
  return 0;
}
